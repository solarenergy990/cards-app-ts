import React from 'react';
import Container from '../Container/Container';

const Header = () => {
  return (
    <Container>
      <h1>Dashboard</h1>
    </Container>
  );
};

export default Header;
